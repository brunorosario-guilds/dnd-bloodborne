name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 * * *' # Executa todo dia a meia noite
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: curl -sSf https://roiqfjrkzajstnmwqrlq.supabase.co/rest/v1/ > /dev/null
